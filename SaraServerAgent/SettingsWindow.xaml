<Window x:Class="SaraServerAgent.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Settings - SARA Server Agent"
        Width="500" Height="420"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource BgDarkBrush}"
        Foreground="{StaticResource TextPrimaryBrush}"
        FontFamily="Segoe UI"
        FontSize="13"
        SnapsToDevicePixels="True">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0" Text="Settings"
                   FontSize="20" FontWeight="SemiBold"
                   Foreground="{StaticResource TextPrimaryBrush}"
                   Margin="0,0,0,16" />

        <!-- Form -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- Project Path -->
                <TextBlock Text="Project Path" Style="{StaticResource SectionTitle}" Margin="0,0,0,4" />
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBox x:Name="txtProjectPath"
                             Grid.Column="0"
                             Background="{StaticResource BgInputBrush}"
                             Foreground="{StaticResource TextPrimaryBrush}"
                             BorderBrush="{StaticResource BorderBrush}"
                             BorderThickness="1"
                             Padding="8,6"
                             FontSize="12" />
                    <Button Grid.Column="1" Content="Browse"
                            Style="{StaticResource SecondaryButton}"
                            Height="32" Width="70" Margin="8,0,0,0"
                            FontSize="11"
                            Click="BrowsePath_Click" />
                </Grid>

                <!-- URL -->
                <TextBlock Text="System URL" Style="{StaticResource SectionTitle}" Margin="0,12,0,4" />
                    <TextBox x:Name="txtUrl"
                         Background="{StaticResource BgInputBrush}"
                         Foreground="{StaticResource TextPrimaryBrush}"
                         BorderBrush="{StaticResource BorderBrush}"
                         BorderThickness="1"
                         Padding="8,6"
                         FontSize="12" />

                <!-- Port -->
                <TextBlock Text="Port" Style="{StaticResource SectionTitle}" Margin="0,12,0,4" />
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="100" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBox x:Name="txtPort"
                             Grid.Column="0"
                             Background="{StaticResource BgInputBrush}"
                             Foreground="{StaticResource TextPrimaryBrush}"
                             BorderBrush="{StaticResource BorderBrush}"
                             BorderThickness="1"
                             Padding="8,6"
                             FontSize="12" />
                    <Button Grid.Column="1" Content="Test Port"
                            Style="{StaticResource SecondaryButton}"
                            Height="32" Width="80" Margin="8,0,0,0"
                            FontSize="11"
                            Click="TestPort_Click" />
                </Grid>

                <!-- Checkboxes -->
                <CheckBox x:Name="chkAutoStart" Content="Start with Windows"
                          Foreground="{StaticResource TextPrimaryBrush}"
                          Margin="0,16,0,0"
                          FontSize="13" />
                <CheckBox x:Name="chkMinimizeToTray" Content="Minimize to tray on close"
                          Foreground="{StaticResource TextPrimaryBrush}"
                          Margin="0,8,0,0"
                          FontSize="13" />
            </StackPanel>
        </ScrollViewer>

        <!-- Buttons -->
        <Grid Grid.Row="2" Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" x:Name="lblStatus"
                       Foreground="{StaticResource TextSecondaryBrush}"
                       VerticalAlignment="Center"
                       FontSize="11" />
            <Button Grid.Column="1" Content="Cancel"
                    Style="{StaticResource SecondaryButton}"
                    Width="100" Height="34" Margin="0,0,8,0"
                    IsCancel="True"
                    Click="Cancel_Click" />
            <Button Grid.Column="2" Content="Save"
                    Style="{StaticResource ActionButton}"
                    Background="#166534"
                    Width="100" Height="34"
                    IsDefault="True"
                    Click="Save_Click" />
        </Grid>
    </Grid>
</Window>
